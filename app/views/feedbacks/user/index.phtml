<div class="flex flex-col items-center w-4/5 mx-auto min-h-screen">
    <a href="<?= Route(name: 'user.feedbacks.new') ?>" class="btn-md bg-primary text-white px-4 py-2 rounded mb-10 block text-center">NOVO</a>

    <?php foreach ($openFeedbacks as $feedback): ?>
        <div class="flex justify-between items-center bg-tertiary text-white p-4 rounded shadow-md mb-4 w-full max-w-md">
            <span class="w-3 h-3 bg-white rounded-full mr-3"></span>
            <h2 class="text-lg font-bold"><?= $feedback->type ?></h2>
            <div class="flex items-center space-x-4"> <!-- Adicionado flex e space-x para alinhamento e espaçamento -->
                <a href="<?= route('user.feedbacks.edit', params: ['id' => $feedback->id]) ?>" class="inline-flex items-center p-1">
                    <i class="fa-regular fa-pen-to-square text-lg leading-none"></i>
                </a>
                <form
                    action="<?= Route(name: 'user.feedbacks.delete', params: ['id' => $feedback->id]) ?>"
                    method="POST" class="flex items-center mb-0 q">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="px-4 py-2 rounded text-red-500 inline-flex items-center">
                        <i class="fa-regular fa-trash-can text-lg leading-none"></i>
                    </button>
                </form>
            </div>
        </div>
    <?php endforeach; ?>
</div>
