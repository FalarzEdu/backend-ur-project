<?php foreach (\Lib\FlashMessage::get() as $type => $msg) : ?>
    <div 
        class="alert-message alert-<?= $type ?>" 
        role="alert"
    >
        <div><?= $msg ?></div>
        <button 
            type="button" 
            class="btn-close" 
            data-bs-dismiss="alert" 
            aria-label="Close"
        ></button>
    </div>
<? endforeach ?>